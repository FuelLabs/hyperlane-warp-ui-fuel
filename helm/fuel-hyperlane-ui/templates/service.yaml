apiVersion: v1
kind: Service
metadata:
  name: {{ include "fuel-hyperlane-ui.fullname" . }}
  labels:
    {{- include "fuel-hyperlane-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fuel-hyperlane-ui.selectorLabels" . | nindent 4 }}
